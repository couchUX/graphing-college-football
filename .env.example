# College Football Data API key (server-side only — never exposed to the browser).
# Used by the /api/cfbd serverless proxy and the Vite dev-server proxy.
# Get your API key from: https://collegefootballdata.com/key
CFB_API_KEY=your_api_key_here
